# ─── Database ───────────────────────────────────────────────
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_URL=postgresql+asyncpg://docsearch:change_me_in_production@postgres:5432/docsearch

# ─── Redis ──────────────────────────────────────────────────
REDIS_PASSWORD=change_me_in_production
REDIS_URL=redis://:change_me_in_production@redis:6379/0

# ─── JWT ────────────────────────────────────────────────────
JWT_SECRET_KEY=generate_with_openssl_rand_hex_32
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ─── AWS S3 ─────────────────────────────────────────────────
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-docsearch-bucket
S3_PRESIGNED_URL_EXPIRY_SECONDS=900

# ─── AI Providers ───────────────────────────────────────────
# Provider selection: groq | openai | anthropic (LLM)
# Provider selection: bgem3 | gemini | openai (EMBED)
# Provider selection: cohere | none (RERANK / FALLBACK)
# Provider selection: pdfplumber | llamaparse | gcv (OCR)
AI_LLM_PROVIDER=groq
AI_EMBED_PROVIDER=bgem3
AI_EMBED_FALLBACK_PROVIDER=cohere
AI_RERANK_PROVIDER=cohere
AI_OCR_PROVIDER=pdfplumber

# Groq (Primary LLM)
GROQ_API_KEY=your_groq_key_here
GROQ_LLM_MODEL=llama-3.3-70b-versatile

# Gemini (Alternative Embeddings)
GOOGLE_API_KEY=your_google_key_here
GEMINI_EMBED_MODEL=text-embedding-004

# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_LLM_MODEL=gpt-4o-mini
OPENAI_EMBED_MODEL=text-embedding-3-small
OPENAI_EMBED_DIMENSIONS=1536

# Anthropic (Alternative LLM)
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_LLM_MODEL=claude-3-5-haiku-20241022

# Cohere (Reranker + Embedding Fallback)
COHERE_API_KEY=your_cohere_api_key
COHERE_RERANK_MODEL=rerank-english-v3.0

# LlamaParse (Alternative OCR)
LLAMAPARSE_API_KEY=llx-...

# Google Cloud Vision (Scanned PDF OCR Fallback)
GOOGLE_APPLICATION_CREDENTIALS_JSON=
GOOGLE_APPLICATION_CREDENTIALS_PATH=

# ─── App ────────────────────────────────────────────────────
APP_ENV=development
CORS_ORIGINS=["http://localhost:3000"]
RATE_LIMIT_PER_USER=60/minute
RATE_LIMIT_PER_TENANT=1000/minute
